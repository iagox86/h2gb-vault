These are a bunch of random Windows and Linux executables, shared libraries,
and static libraries, for both 32- and 64-bits. They are pretty simple (as in,
they're from the OS or common software), so there shouldn't be any weird tricks
afoot.
